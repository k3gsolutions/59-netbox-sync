# Sub-agent briefs

These briefs define the project specialties in a reusable format for agent delegation.
They mirror the same operational focus used by `skills/`, `prompts/`, and `tools/local/`.

Use them as task roles when delegating review, documentation, compliance, or modeling work.
